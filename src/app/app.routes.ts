import { Routes } from '@angular/router';
import { StockListComponent } from './stock-list/stock-list.component';

export const appRoutes: Routes = [
  { path: '', component: StockListComponent },  // Default route for stock data
];

